{
  "name": "document-approval-portal",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix mock-api\" \"npm run dev --prefix web\"",
    "install:all": "npm install --prefix mock-api && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
